{
    "name": "circuits",
    "private": true,
    "scripts": {
        "start": "yarn run:v3 && yarn run:eddsa && yarn run:smt",
        "run:v3": "./circom semaphore-v3/semaphore.circom --r1cs",
        "run:eddsa": "./circom semaphore-eddsa/semaphore.circom --r1cs",
        "run:smt": "./circom semaphore-smt/semaphore.circom --r1cs"
    },
    "dependencies": {
        "circomlib": "^2.0.2"
    }
}
